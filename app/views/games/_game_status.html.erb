<div id="game-status" class="text-center mb-4">
  <p class="text-sm text-gray-600">
    Turn: <%= game.current_turn == current_user.id ? "Your" : "Opponent's" %> |
    Phase: <%= game.turn_phase.titleize %>
  </p>
</div> 