<div class="pt-3 border-t border-slate-500/50 flex justify-center mx-auto mb-5" data-controller="bot-selection">
  <%= form_with(model: @game, local: true, class: "") do |f| %>
    <h1 class="mb-4 text-xl font-medium text-center text-white">Choose Opponent</h1>
    <%= f.hidden_field :bot_difficulty, 
        data: { bot_selection_target: "difficulty" } %>
    
    <div class="mb-6 w-full sm:w-96 mx-auto text-sm flex flex-row gap-2 text-center">
      <div class="flex-1">
        <div data-bot-selection-target="botCard"
             data-difficulty="easy"
             data-action="click->bot-selection#select"
             class="cursor-pointer h-[4.5rem] flex flex-col justify-center items-center mb-3 !bg-white px-2 rounded-lg shadow-xl hover:bg-slate-800">
          <h2 class="relative text-lg font-semibold text-slate-700">Easy</h2>
        </div>
        <p class="text-slate-300 text-xs">Plays a random card in any column.</p>
      </div>
      
      <div class="flex-1">
        <div data-bot-selection-target="botCard"
             data-difficulty="medium"
             data-action="click->bot-selection#select"
             class="cursor-pointer h-[4.5rem] flex flex-col justify-center items-center mb-3 bg-transparent border border-slate-500/60 px-2 rounded-lg">
          <h2 class="relative text-lg font-semibold text-white">Medium</h2>
        </div>
        <p class="text-slate-300 text-xs">Plays to strengthen specific hands.</p>
      </div>
      
      <div class="flex-1">
        <div data-bot-selection-target="botCard"
             data-difficulty="hard"
             data-action="click->bot-selection#select"
             class="relative overflow-hidden cursor-pointer h-[4.5rem] flex flex-col justify-center items-center mb-3 bg-transparent border border-slate-500/60 px-2 rounded-lg">
          <h2 class="text-white text-lg font-semibold leading-6">Hard</h2>
        </div>
        <p class="text-slate-300 text-xs">Tries to block your strongest hands.</p>
      </div>
    </div>

    <div class="flex flex-col px-1 sm:flex-row sm:mx-auto sm:w-96 justify-center gap-2 font-medium">
      <%= f.submit "Start Game", class: "btn btn-primary" %>
      <%= link_to "Cancel", games_path, class: "btn btn-outline" %>
    </div>
  <% end %>
</div> 