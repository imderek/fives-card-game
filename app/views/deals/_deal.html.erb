<div id="<%= dom_id deal %>">
  <h3 class="mb-4 text-2xl font-semibold text-gray-900"><%= deal.name %> 
    <span class="ml-1 relative top-[-2px] inline-flex items-center px-2.5 py-0.5 rounded-full text-sm font-medium border bg-gray-100 text-gray-800">
      <%= deal.status.titleize %>
    </span>
  </h3>
  <p class="text-gray-600"><%= deal.description %></p>
</div>
